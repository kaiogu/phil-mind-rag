#!/usr/bin/env bash
# pre-commit: runs ruff and ty before every commit.
# Install once: git config core.hooksPath .githooks
set -euo pipefail

echo "🔍 ruff check..."
uv run ruff check .

echo "🔍 ty check..."
uv run ty check

echo "✅ pre-commit checks passed"
